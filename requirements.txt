# Week 2 dependencies — bounded to avoid surprise breaking changes mid-course.
# Lower bound: minimum version we rely on. Upper bound: next major (may break).
fastapi>=0.100.0,<1.0.0
uvicorn>=0.20.0,<1.0.0
pydantic>=2.0.0,<3.0.0
openai>=1.0.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
groq>=1.0.0,<2.0.0

# Week 3 — dependencies for agent_teaser.py (the Lab 3D finale).
# The backend/ and frontend/ folders each have their own requirements.txt;
# this one is only for running the agent teaser locally against your deployed API.
openai>=1.0.0,<2.0.0
requests>=2.28.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0